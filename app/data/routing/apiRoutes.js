var path = require("path");


module.exports = function (app) {

    app.get("/api/friends", function (req,res){
        
    })

    app.post("/api/friends", function (req, res) {

        friendslistData = [];
        res.json(true)
    }

    );

}; // You need to have apps in scope.

